The *.csh scripts in this directory are used to add necessary packages to
  the users path
  
These can be called from a user's .cshrc file (see the file "cshrc" for
  an example).  The user may also have a .alias file (see "alias for example).
  
startup_MMPS.m is a matlab script that adds MMPS and other relevant
  functions to the matlab path, and can be called at startup with a
  file startup.m in /home/<user>/matlab (see matlab/startup.m for example) 

The functions in matlab/MMPS_scripts are examples of how
  to setup for customized processing with MMPS

